'use client';

import React from 'react';
import '../styles/pages.css';

/**
 * StyleManager component
 * This is now simplified since we're directly importing the CSS,
 * but we keep the component for future optimizations if needed
 */
export default function StyleManager() {
  // CSS is now imported at the top of the file
  // which is a more reliable method of loading styles in Next.js
  return null;
}
